local CanTeleport = false

task.spawn(function()
    while true do
        repeat task.wait() until game.Players.LocalPlayer.Character
        
        local char = game.Players.LocalPlayer.Character
        if not char:FindFirstChild("entered") and char:FindFirstChild("HumanoidRootPart") then
            repeat task.wait()
                firetouchinterest(char:WaitForChild("Head"), workspace.Lobby.Teleport1.TouchInterest.Parent, 0)
                firetouchinterest(char:WaitForChild("Head"), workspace.Lobby.Teleport1.TouchInterest.Parent, 1)
            until char:FindFirstChild("entered")
            CanTeleport = true
        end
        
        task.wait()
    end
end)

task.spawn(function()

while true do
if CanTeleport then
local Character = game.Players.LocalPlayer.Character
local Glove = workspace:FindFirstChild("djdalpoip"):FindFirstChildOfClass("Tool")

Character.PrimaryPart.CFrame = Glove.Glove.CFrame

if Glove.Name == "KillStreak" or Glove.Name == "Rage" then

task.wait(2)

local Character = game.Players.LocalPlayer.Character

if Character.Humanoid.Health > 0 then
Character.PrimaryPart.CFrame = workspace.DEATHBARRIER.CFrame
CanTeleport = false
end

end

if Character.Humanoid.Health > 0 then
CanTeleport = false
end

end

task.wait()

end

end)
